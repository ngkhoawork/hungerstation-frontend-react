// import { fromJS } from 'immutable';
// import { selectRegistrationPageContainerDomain } from '../selectors';

describe('selectRegistrationPageContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
