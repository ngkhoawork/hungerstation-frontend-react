import styled from 'styled-components';
import { flexBox } from 'utils/styles';

const StyledPageNumbers = styled.div`
  ${flexBox({ align: 'center' }, `margin-top: 4px;`)};
`;

export default StyledPageNumbers;
