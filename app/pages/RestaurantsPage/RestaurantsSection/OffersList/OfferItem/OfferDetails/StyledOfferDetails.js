import styled from 'styled-components';
import { flexBox } from 'utils/css/styles';

const StyledOfferDetails = styled.div`
  ${flexBox({ justify: 'flex-start', direction: 'column' })};
`;

export default StyledOfferDetails;
